/**
 * Compatibility shim.
 *
 * Guided planning replaced the old planner chat surface in PF-169 while the
 * existing route name stays stable.
 */
export { GuidedPlanningScreen as PlannerChatScreen } from './GuidedPlanningView';
